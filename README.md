# terraform-s3-dynamodb-backend
Production-style Terraform backend using AWS S3, DynamoDB state locking, versioning, and workspaces.

This repository is intended for team use so everyone works from a shared and consistent Terraform state.
